<?xml version="1.0" encoding="utf-8"?>
<odoo>

<record id="resource_calendar_view_form" model="ir.ui.view">
        <field name="name">resource.calendar.view.form</field>
        <field name="model">resource.calendar</field>
        <field name="inherit_id" ref="resource.resource_calendar_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='hours_per_day']" position="after">
                <field name="penalty_sign_in"/>
                <field name="penalty_sign_out"/>
            </xpath>

        </field>
    </record>

</odoo>