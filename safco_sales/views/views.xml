<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record model="ir.ui.view" id="view_order_form_safco">
            <field name="name">sale.order</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">
<!--                <button name="action_quotation_send" position="after">-->
<!--                    <button name="gm_action_confirm"-->
<!--                            string="GM Confirm" type="object" groups="sales_team.group_sale_manager"-->
<!--                            attrs="{'invisible': [('state', 'not in', ['waiting'])]}"/>-->
<!--                    <button name="action_cancel" string="Cancel Quote" type="object" attrs="{'invisible': [('state', 'not in', ['waiting'])]}"/>-->
<!--                </button>-->
                <field name="payment_term_id" position="after">
                    <field name="user_id"/>
                    <field name="warehouse_id"/>
                    <field name="client_order_ref"/>
                </field>
            </field>
        </record>
    </data>
</odoo>